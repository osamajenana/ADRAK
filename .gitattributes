# Normalise line endings on commit. Python-based edits on Windows write CRLF, Pint wants LF,
# and without this the two fight over every file forever.
* text=auto eol=lf

*.png binary
*.jpg binary
*.woff2 binary
*.mp4 binary
